@model string
@using (Html.BeginForm("Search", "Recipe", FormMethod.Get, new { @class = "form-horizontal" }))
{
    <div class="form-group">
        <div class="col-md-6">
            <input type="text" id="input" name="input" class="form-control" />
        </div>
        <div class="col-md-6">
            <button type="submit" class="btn btn-primary" id="submitBtn">Търси</button>
        </div>
    </div>
}